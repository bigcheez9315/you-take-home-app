apiVersion: v1
kind: Service
metadata:
  name: you-take-home-app-service
  annotations:                                                                                                                                                                                                                                                                        
    service.beta.kubernetes.io/aws-load-balancer-type: "elb"  
  labels:
    app: you-app 
spec:
  selector:
    app: you-app 

  ports:
    - port: 5000
      targetPort: 5000                

  type: LoadBalancer
